# PostgreSQL Database Connection String (e.g. Neon, Supabase, Vercel Postgres)
# Replace with your actual connection URL
DATABASE_URL=postgresql://postgres:password@localhost:5432/cn_vlab?sslmode=require

# JWT Secret for Session Signing
JWT_SECRET=super_secret_vlab_jwt_key_2026

# Faculty Registration Authorization Secret (Required for creating faculty accounts)
FACULTY_SECRET=your_institution_faculty_secret_2026

# Node Environment
NODE_ENV=development
